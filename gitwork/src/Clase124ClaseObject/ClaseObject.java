package Clase124ClaseObject;
/*======================================================CLASE OBJECT====================================
 * 
 * Todas las clases heredan de la clase objcect, ya que es la clase rais de Java
 * -Se encuentra en el paquete java.lang, el cual es el paquete principal de java 
 * -No es necesario hacer un import de las clases que pertenescan a la clase java.lang
 * 
 * La importanci a de la clase object, recae en que todas las clases heredan de manera directa o indirecta de la clase object
 * de esta manera el lenguaje java comparte esto en comun con todas las clases en java lo que permite asegurar que algunas
 * funciones o caracteristicas sean compartidas entre todas las clases en java.
 * Los metodos mas importantes que usaremos de la clase objecto son(los usaremos al sobreexcribirlos en nuestras clases):
 * 
 * 	toString() = String 
 *  equals() = boolean
 * 	hashCode() = int
 * 
 * 
 * */
public class ClaseObject {

}
